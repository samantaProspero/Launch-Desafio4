<div class="item">
    <div>Avatar URL</div>
    <div>
        <input type="url" name="avatar_url" placeholder="http://" value="{{teacher.avatar_url}}" >
    </div>
    </div>
    <div class="item">
        <div>Nome</div>
        <div>
            <input type="text" name="name" placeholder="Nome do Professor" value="{{teacher.name}}"  >
        </div>
    </div>
    <div class="item">
        <div>Data de Nascimento</div>
        <div><input type="date" name="birth" value="{{teacher.birth}}"></div>
    </div>
     <div class="item">
        <div>Grau de Escolaridade</div>
        <div>
            <select name="grau">
                <option value="Ensino Médio Completo"
                    {% if student.blood =="Ensino Médio Completo"%}selected{%endif%}>Ensino Médio Completo
                </option>
                <option value="Ensino Superior Completo"
                    {% if student.blood =="Ensino Superior Completo"%}selected{%endif%}>Ensino Superior Completo
                </option>
                <option value="Mestrado"
                    {% if student.blood =="Mestrado"%}selected{%endif%}>Mestrado
                </option>
                <option value="Doutorado"
                    {% if student.blood =="Doutorado"%}selected{%endif%}>Doutorado
                </option>
            </select>
        </div>
    </div>  {# <input type="select"  value="{{teacher.grau}}"  > #}
    <div class="item">
        <div>Tipo de Aula</div>
        <div>
            <span>
                <input 
                    type="radio" 
                    name="tipo"
                    value="P" 
                    {% if teacher.gender == "P" %}
                    checked
                    {% endif %} 
                    >Presencial
            </span>
            <span>
                <input 
                    type="radio" 
                    name="tipo"
                    value="D"
                    {% if teacher.gender == "D" %}
                    checked
                    {% endif %}
                    >À distância
            </span>
        </div>
    </div>
    <div class="item">
        <div>Acompanhamento</div>
        <div>
            <input type="text" name="services" placeholder="Separe as matérias por vírgula" value="{{teacher.services}}">
        </div>
    </div>
    <button type="submit">Salvar</button>